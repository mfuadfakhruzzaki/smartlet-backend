# Production Environment Configuration
# Copy this file to .env.prod and update with your production values

# Database Configuration (Production)
DB_USER=swiflet_prod_user
DB_PASSWORD=your-secure-production-db-password
DB_NAME=swiflet_production
DB_SSLMODE=require

# TimescaleDB Configuration (Production)
TIMESCALE_USER=swiflet_timescale_user
TIMESCALE_PASSWORD=your-secure-timescale-password
TIMESCALE_DB=swiflet_timeseries_prod
TIMESCALE_SSLMODE=require

# JWT Configuration (Production)
JWT_SECRET=your-super-secure-jwt-secret-key-at-least-32-characters
JWT_EXPIRY=24h

# MQTT Configuration (Production)
MQTT_CLIENT_ID=swiflet-backend-prod
MQTT_USERNAME=swiflet_mqtt_user
MQTT_PASSWORD=your-secure-mqtt-password
MQTT_TOPIC_SENSOR=sensors/+/data
MQTT_TOPIC_CONTROL=control/+/command

# Redis Configuration (Production)
REDIS_PASSWORD=your-secure-redis-password

# Optional: External MQTT broker (if not using Docker MQTT)
# MQTT_BROKER=tcp://your-external-mqtt-broker:1883