# Development Dockerfile dengan hot reload menggunakan Air
FROM golang:1.21-alpine AS dev

WORKDIR /app

# Install air untuk hot reload
RUN go install github.com/cosmtrek/air@latest

# Install dependencies untuk development
RUN apk add --no-cache git curl wget

# Copy go mod files
COPY go.mod go.sum ./
RUN go mod download

# Copy source code
COPY . .

# Expose port
EXPOSE 8080

# Default command untuk development (hot reload)
CMD ["air"]